{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "uv run python manage.py runserver 0.0.0.0:8000",
    "dev:stream": "uv run uvicorn config.asgi_stream:application --host 0.0.0.0 --port 8001 --reload",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "typecheck": "uv run mypy .",
    "test": "uv run pytest && uv run python ../../scripts/check_coverage.py"
  }
}
